import { CovidDataFormatDirective } from './covid-data-format.directive';

describe('CovidDataFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CovidDataFormatDirective();
    expect(directive).toBeTruthy();
  });
});
