import { DisplayCovidDataPipe } from './covid-data-pipe';

describe('DisplayCovidDataPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayCovidDataPipe();
    expect(pipe).toBeTruthy();
  });
});
